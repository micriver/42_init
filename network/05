# 05 [COMMAND] 
# Identify the IP address of the DNS that responds to the following url: slash16.org
# nslookup slash16.org | tail -n 3
nslookup slash16.org | tail -n 3 | grep :

# nslookup is a network administration command-line tool used for querying the Domain Name System (DNS) to obtain domain name or IP address mapping, or other DNS records. The name "nslookup" means "name server lookup". 
# The Hash, and number after it, is the default TCP/UDP name server port
